I3Pruner
~~~~~~~~~~~~~~~~~~
The purpose of the I3Pruner is to remove (or prune) all DOM launches that fall outside of the readout windows.  This must be run after the global trigger simulation, since that's the module that determines the length and locations of the readout windows.


