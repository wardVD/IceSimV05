#!/usr/bin/env python
import logging
logging.basicConfig()
rootLogger = logging.getLogger('')
rootLogger.setLevel(logging.INFO)

from icecube.simprod.modules import PolyplopiaModule

if __name__ == '__main__':
   stats = {}
   ptprop = PolyplopiaModule()
   ptprop.ExecuteOpts(stats)
